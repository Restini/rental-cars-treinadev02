<h1>Registrar novo Fabricante</h1>

<%= form_for Manufacturer.new do |f| %>
<%= f.label :name, 'Nome' %>
<%= f.text_field :name %>
<%= f.submit 'Enviar' %>
<% end %>

<%= link_to 'Fiat', manufacturers_path %>