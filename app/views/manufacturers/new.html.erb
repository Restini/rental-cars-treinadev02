<h1>Registrar novo Fabricante</h1>
<%= link_to 'Fiat', new_fiat_manufacture_path %>

<%= form_for @manufacturer do |f| %>
<%= f.label :name, 'Nome' %>
<%= f.text_field :name %>
<%= f.submit 'Enviar' %>
<% end %>